export interface IPayioApp {
  // #region Properties (10)

  active: boolean;
  description: string;
  downloadUrl: string;
  features: string[];
  id: string;
  name: string;
  slug: string;
  price: number;
  secrets: string;
  tags: string[];

  // #endregion Properties (10)
}
