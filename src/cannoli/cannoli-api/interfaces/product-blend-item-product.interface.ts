export interface ProductBlendItemProduct {
    id: string; // Id do item no array
    categoryId: string;
    categoryName: string;
    productId: string;
    productName: string;
    sequence: number;
}
