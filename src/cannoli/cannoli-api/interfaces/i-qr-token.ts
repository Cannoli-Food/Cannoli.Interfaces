// Set as deprecated class

export interface IQrToken {
    // #region Properties (8)

    active: boolean;
    createdAt: Date;
    id: string;
    menuId: string;
    merchantId: string;
    publishAt: Date;
    updatedAt: Date;
    views: number;

    // #endregion Properties (8)
}
