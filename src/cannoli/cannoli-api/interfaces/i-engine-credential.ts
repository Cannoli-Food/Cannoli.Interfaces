export interface IEngineCredential {
    // #region Properties (3)

    
    password: string;
    
    storeId?: string;
    
    username: string;
    
    autoConfirm?: boolean;

    // #endregion Properties (3)
}
